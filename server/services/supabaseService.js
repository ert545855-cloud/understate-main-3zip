/**
 * supabaseService.js — Replit PostgreSQL proxy
 * Supabase kaldırıldı. Tüm fonksiyonlar dbService üzerinden çalışır.
 * Bu dosya geriye dönük uyumluluk için korunur.
 */
module.exports = require('./dbService');
